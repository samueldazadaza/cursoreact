import React, {Fragment, useState} from 'react'

const Contador = () => {
  return (
    <Fragment>
        <h2>Cantidad del contador:</h2>
        <button>Aumentar</button>
        <button>Reducir</button>
    </Fragment>
  )
}

export default Contador
